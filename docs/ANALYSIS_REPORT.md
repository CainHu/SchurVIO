# 视觉后验作用、协方差与噪声敏感度分析

配套交互式报告：`out/report.html`（自包含 HTML，浏览器直接打开）。

## 生成方法

```bash
cmake --build cmake-build-release --target VinsAnalysis VinsReport
./cmake-build-release/VinsAnalysis.exe 400 base 1.0
./cmake-build-release/VinsReport.exe
```

报告包含总体精度、可旋转 3D 轨迹、GT/EST/特征点/机体坐标系、视觉更新
先验与后验、NEES/NIS 一致性、误差与协方差、零偏与重力、噪声扫描、更新耗时及
观测量统计。交互式 Canvas 固定使用逻辑高度，并通过 `requestAnimationFrame`
合并拖拽/滚轮重绘，避免高 DPI 屏幕上的尺寸累乘和事件洪泛。

## 一、协方差负值的真正原因与修复

旧版本在 100 秒仿真中有 `142/2001` 帧的速度协方差块 trace 为负，首次出现于
约 92.9 秒。最初将其归因于视觉更新中的 Joseph 等价形式，这是不准确的。

分阶段诊断得到：

- 负 trace 在视觉更新之前已经存在；视觉更新首次引入负 trace 的次数为 0。
- 联合协方差的最小特征值从约 0.8 秒起逐渐变负，而视觉更新前后几乎不变。
- 根因在 IMU 预测：旧代码只传播左上角 18×18 的 INS 协方差，却没有传播
  INS 与滑窗增广位姿之间的互协方差。

联合状态写成

```text
P = [ P_ii  P_ic ]
    [ P_ci  P_cc ]
```

预测时增广位姿保持不变，完整状态转移为 `F = diag(A, I)`，因此必须执行：

```text
P_ii' = A P_ii A^T + Q
P_ic' = A P_ic
P_ci' = P_ic'^T
P_cc' = P_cc
```

旧实现只更新 `P_ii`，却冻结 `P_ic`。这些块不再描述同一个随机变量经过同一状态
转移后的联合分布，因此整个 198×198 协方差会变成不定矩阵。

修复后同步传播互协方差。左上角 `P_ii` 既可以使用显式合同变换，也可以继续
使用原来的分块展开；两者在数学上都是 `A P_ii A^T`。原来的 `else` 分支对
18×18 的 `P_ii` 计算本身是正确的，问题是三条实现路径此前都没有同步更新
`P_ic`，所以对完整 198×198 联合协方差而言并不完整。三条路径现在分别按自身
结构补全传播：默认路径显式计算 `A P_ic`；`CONFIG_DEBUG` 用分块公式计算完整的
18×198 顶部块 `A [P_ii P_ic]`；优化 `else` 对联合协方差的相关列直接执行
`P F^T`，得到 `P_ci A^T` 后通过对称性恢复 `P_ic`。后两条路径均不构造 `A`。
结果：

| 指标 | 修复前 | 修复后 |
|---|---:|---:|
| trace 为负的帧数 | 142 / 2001 | **0 / 2001** |
| 联合协方差最小特征值 | 约 -40 | **约 -2e-12** |
| 位置 RMSE | 0.1058 m | **0.0701 m** |
| 速度 RMSE | 0.0216 m/s | **0.0133 m/s** |
| 位置改善率 | 50.5% | **81.1%** |

`-2e-12` 相对协方差矩阵尺度是正常的浮点舍入误差，不是数值不定。两轮稳定
计时中，完整互协方差传播与旧实现的总运行时间均约 11.2 秒，没有可测量开销。

三条 `P_ii` 实现路径的回归结果如下。`CONFIG_DEBUG` 还会启用调试版状态积分，
因此其运行时间和轨迹会有轻微差异，不能把差异全部归因于协方差实现。

| `P_ii` 路径 | 位置 RMSE | 视觉更新改善率 | 负协方差告警 |
|---|---:|---:|---:|
| 显式 `A P A^T`（默认） | 0.0701 m | 81.1% | 0 |
| 原分块展开 `else` | 0.0699 m | 81.2% | 0 |
| `CONFIG_DEBUG` | 0.0696 m | 81.5% | 0 |

## 二、Joseph 形式是否需要替换

当前视觉标量序贯更新已经是 Joseph 形式的代数等价展开：

```text
P+ = (I-KH) P (I-KH)^T + K R K^T
```

Joseph 形式的目的正是比简化式 `P+ = (I-KH)P` 更好地保持对称性和半正定性。
本次诊断证明视觉更新不是负协方差的来源，因此改成简化式不会解决问题，反而会
降低数值稳定性；直接改成平方根滤波也属于高成本绕过根因。

修复联合预测后，当前 Joseph 等价实现可以保留。若将来仍出现超出 `1e-10` 相对
量级的负特征值，再考虑平方根协方差或 UD 滤波。

## 三、视觉后验修正作用

修复后基线（`uv_var=400`, `proc_scale=1`）：

| 指标 | 数值 |
|---|---:|
| 位置 RMSE | 0.0699 m |
| 速度 RMSE | 0.0133 m/s |
| 姿态 RMSE | 0.000731 rad（0.0419°） |
| 最大位置误差 | 0.1298 m |
| 视觉更新让位置更接近 GT 的比例 | **81.2%** |

平均视觉位置修正量仍小于全局位置误差。这部分主要来自 VIO/SLAM 的 gauge 自由度：
视觉不能压缩全局平移、全局旋转和尺度方向，故全局坐标下的位置协方差仍会增长。
这不等同于协方差失去正定性；前者是可观测性问题，后者是数值/实现错误。

### 一致性统计

报告新增了基于完整协方差块求解的 NEES，以及 Schur 序贯伪量测的 NIS：

| 指标 | 当前基线 | 理论期望 |
|---|---:|---:|
| 联合 Q/P/V NEES / 9 | 约 5.0e-4 | 约 1 |
| NIS / 标量自由度 | 约 8.9e-9 | 约 1 |

两者都远小于 1，说明当前仿真配置明显偏保守；尤其 `uv_var=400` 与归一化图像坐标
残差的尺度不匹配，使视觉量测权重偏低。这比只观察末帧误差是否落在
`3*sqrt(trace(P))` 内更有诊断价值。需要注意，当前结果来自一条具有时序相关性的
仿真轨迹，阈值用于工程诊断；正式统计置信区间应使用多次独立 Monte Carlo 运行。

## 四、修复后的噪声敏感度

旧实现中 `uv_var=200`、`proc_scale=2` 会断崖发散。补全互协方差传播后，这些
发散均消失，说明旧“稳定边界”主要是错误协方差结构造成的，而非传感器噪声本身。

### 量测噪声

| uv_var | 位置 RMSE |
|---:|---:|
| 4 | 0.0309 m |
| 10 | 0.0326 m |
| 25 | 0.0429 m |
| 100 | 0.0586 m |
| 200 | 0.0640 m |
| 400（默认） | 0.0701 m |
| 1600 | 0.0846 m |
| 6400 | 0.1259 m |

扫描范围全部稳定。当前仿真中减小 `uv_var` 会提高视觉权重并改善精度，但不能直接
据此把真实系统参数设成 4；应根据像素噪声、归一化坐标尺度及残差统计重新标定。

### 过程噪声缩放

| scale | 位置 RMSE |
|---:|---:|
| 0.5 | 0.0762 m |
| 1.0（默认） | 0.0701 m |
| 2.0 | 0.0644 m |
| 5.0 | 0.0578 m |
| 10.0 | 0.0514 m |
| 20.0 | 0.0423 m |
| 100.0 | 0.0286 m |

扫描范围也全部稳定。更大的过程噪声使滤波器更愿意接受视觉修正，在当前使用真值
landmark 的理想仿真里持续改善结果。真实系统不能照搬该极值，应通过 IMU Allan
方差、残差白化和 NEES/NIS 一致性来标定。

## 五、结论

1. 协方差负值不是 Joseph 形式造成的。
2. 根因是预测阶段漏掉 `P_INS,clone` 的传播。
3. 补全联合协方差传播后，负 trace 清零，协方差只剩机器精度量级的微小负值。
4. 修复同时显著提升了轨迹精度和视觉后验的修正方向正确率。
5. 旧噪声扫描结论已经失效，报告和本文均已使用修复后的数据重新生成。

## 相关

- [HPP_NULLSPACE.md](HPP_NULLSPACE.md) —— 视觉信息矩阵的结构性零空间
- [HLL_STRUCTURE.md](HLL_STRUCTURE.md) —— landmark 深度弱可观方向
- [README.md](README.md) —— 优化文档总览
